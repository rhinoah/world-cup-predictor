# --- datos crudos y artefactos regenerables (se arman con `python run.py setup`) ---
# Anclados con / a la raiz: sin la barra, `data/` tambien se comia prode/data/,
# que es un PAQUETE del codigo (su __init__.py quedo fuera del repo publicado).
/data/
/output/
/flags/
__pycache__/
*.pyc

# --- datos personales del prode (versionar solo *.example.csv si se crean) ---
pronosticos.csv
pronosticos_detalle.json
predicciones_jornada.txt
update_log.txt
prode_error.log

# --- config local de la maquina / Claude Code ---
.claude/
prodes.json

# --- working docs internos (citan rutas locales) ---
TODO.md
